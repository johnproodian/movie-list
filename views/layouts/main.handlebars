<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies!</title>
    <link rel="stylesheet" href="../../public/stylesheets/output.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-midnight">
    <div class="wrapper">
        <header>
            <h1 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                <a href="/">Movies to Watch</a>
            </h1>
            <nav>
                {{!-- {{#if loggedIn}} --}}
                <a href="/login">login</a>
                <button id="logout" class="btn-no-style">logout</button>
                {{!-- {{else}} --}}
                
                {{!-- {{/if}} --}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
    </div>

    <footer class="fixed bottom-0 text-center w-full bg-#d4d4d8">
        <p><strong>Created by:</strong> 'All of our github usernames as links'</p>
    </footer>
    

    {{!-- {{#if loggedIn}} --}}
    <script src="/javascript/logout.js"></script>
    {{!-- {{/if}} --}}
</body>

</html>